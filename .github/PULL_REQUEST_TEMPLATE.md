## 变更检查

- [ ] 已确认此改动是否也适用于 Windows 本地版。
- [ ] 若属于共享功能，已同步更新 `G:\Wardogs` 本地版，或已记录暂缓事项及原因。
- [ ] 射表、地图坐标、目标/落点/校射和历史 JSON 格式仍与本地版兼容。
- [ ] 已在触控视口检查主要操作，并运行 `npm test` 与 `npm run build`。

同步状态：

<!-- 已同步 / 无需同步（说明原因）/ 暂缓（链接跟踪事项） -->
