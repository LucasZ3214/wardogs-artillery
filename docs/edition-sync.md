# 本地版与静态版同步规则

- 2026-09-18：彩色底图饱和度进一步降至65%，两版同步；本次发布包含左下角地图控件、gzip自动识别及三图默认向东/北各27米的显示校准，不包含道路和简化地图实验。

- 2026-09-18：新增按地图保存的等高线平移校准，单位米，正东/正北，一键归零；只移动显示，不修改高程、底图与火控坐标。已同步两版。

- 2026-09-18：地图、底图图层与等高线控件移动至左下角独立面板，武器/弹道保留左上角，已同步两版；预留底部模式条和safe-area空间。

本项目同时维护两个版本：

- Windows 本地版：`G:\Wardogs`
- GitHub Pages 静态版：`G:\Wardogs\static-site`

## 必须双向同步的功能

以下功能属于共享火控核心；任一版本修改后，都必须检查并同步另一版本：

- L81、SPH-2 射表、有效射程及高低弹道插值
- 方位、距离和地图坐标换算
- 地图边界、地标及瓦片坐标
- Canvas 绘制、准星、射程圈、网格和 Pointer Events 触控状态机
- 目标、落点、迭代虚拟瞄准点和撤销逻辑
- 目标历史的 `schema: 1 / settings / targets` JSON 格式
- 响应式布局、iPad safe-area、全屏和无障碍改进

## 版本专属功能

仅 Windows 本地版维护 FastAPI、SQLite、LAN 配对、WebSocket、PyQt6 悬浮窗、Qt TTS 和本地地图缓存。

仅静态版维护 GitHub Pages 工作流、社区 CDN 地图加载、`localStorage` 和作战区高程派生包。

## 开发记录要求

每项本地版新功能或修复必须在变更说明中写明：

1. 是否适用于静态版；
2. 若适用，是否已经同步；
3. 若无需同步，说明版本边界原因；
4. 若暂缓同步，创建可追踪事项并附链接或编号。

静态版的 Pull Request 模板包含相同检查项。共享逻辑应尽量保持函数签名、数据字段和测试用例一致，避免两个版本产生不可见的算法分叉。

- 2026-09-17：灰度/彩色地图图层切换适用于两版，已同步；离线缓存下载参数仅属于 Windows 本地版。
- 2026-09-17：静态版目标历史补齐方位/距离/仰角，并改为直接删除，与本地版操作逻辑同步。
- 2026-09-17：落点模式取消距离阈值，任意偏差均归入当前目标；仅目标模式创建新目标，已同步两版。
- 2026-09-17：彩色地图使用原始瓦片色彩，仅灰度地图应用亮度与对比度增强，已同步两版。
- 2026-09-17：目标历史加入卡片内重命名、保存与取消操作，已同步两版。
- 2026-09-17：彩色地图恢复高亮准星与射程圈，并增加深色外描边形成双层对比；灰度模式维持单层高亮配色，已同步两版。
- 2026-09-17：缩细彩色地图准星、中心点及射程圈外描边，减少对地图细节的遮挡，已同步两版。
- 2026-09-17：再次缩细彩色地图外描边：准星外层约增加 1.5px，射程圈外层 2.5px，已同步两版。
- 2026-09-17：加入固定直径圆形 Control Zone：可直接放置/拖动圆心，也可选取两个圆边点反推固定半径圆；双解时默认选择更靠近塔楼群几何中心的一侧，并保留“翻转 CZ”切换镜像解。CZ 按地图保存在浏览器本地，已同步两版。
- 2026-09-17：CZ 绘制模式移至右下角独立浮动组，已同步两版。
- 2026-09-18：CZ 当前试用尺寸调整为直径 1100m、半径 550m，两点定圆最大弦长同步为 1100m，已同步两版。
- 2026-09-18：彩色底图饱和度降为75%，已同步两版。静态版先发布独立10/5/2m矢量等高线开关；本地版现有等高线实现保留，矢量加载器和新LOD同步暂缓（跟踪：LOCAL-CONTOUR-LOD），简化地图不在此次发布范围。
